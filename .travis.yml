language: generic
sudo: required
services:
    - docker

before_install:
    - docker build -t alfredmaboa/Docker-React -f Dockerfile.dev .

script:
    - docker run -e CI=true alfredmaboa/Docker-React npm run test